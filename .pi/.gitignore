# Fabric-Pi OpenCode-to-Pi semantic port — machine-local runtime state.

# Pi project package caches
npm/
git/
node_modules/

# Fabric mesh and actor runtime
fabric/
runtime/

# Per-session summary generated by the native session-summary extension
state/session-summary.md

# Runtime artifacts (memory, todo, per-run review trails)
artifacts/
