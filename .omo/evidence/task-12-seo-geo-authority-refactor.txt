T12 — Shared authority components (Byline / EvidenceNote / FreshnessNotice / RelatedContent)
=============================================================================================

Deliverables (import-only over T7/T8 kernels; NOT wired into any page — that is T14/T15):
- src/components/authority.ts          — pure decision seam delegating to the kernels
- src/components/Byline.astro          — renders PERSON_ENTITY (id "ryan-brosas")
- src/components/EvidenceNote.astro    — renders ONLY approved, public-safe claim source refs
- src/components/FreshnessNotice.astro — renders a date ONLY for a substantive change (INV-13)
- src/components/RelatedContent.astro  — renders getRelatedList() (public targets only; [] → nothing)
- tests/components.test.mjs            — seam tests proving the render decisions

Design note: node --test cannot import .astro, so each component's decision is a thin
pure function in src/components/authority.ts (delegates to resolvePublicClaim / nextModifiedAt /
getRelatedList / PERSON_ENTITY — never reimplements policy). The .astro files are render-only
wrappers using the proven `{ list.length > 0 && (...) }` / `{ x !== undefined && (...) }`
guard pattern (same as Breadcrumbs.astro), so an empty/undefined decision renders NOTHING.
No inline <script> in any component (shell.test.mjs:assertOneNavScript stays satisfied).

--- RED (before src/components/authority.ts existed) --------------------------------------
$ node --test tests/components.test.mjs
Error: Cannot find module '.../src/components/authority.ts'
  code: 'ERR_MODULE_NOT_FOUND'
✖ tests 1  pass 0  fail 1        (fails for the RIGHT reason — seam not yet created)

--- GREEN (my test in isolation) ----------------------------------------------------------
$ node --test tests/components.test.mjs
✔ authority — Byline (renders ryan-brosas)
✔ authority — EvidenceNote (approved → source ref; blocked/retired/internal-only → [])
✔ authority — FreshnessNotice (substantive → date; typo-fix/formatting → undefined)
✔ authority — RelatedContent (public kept; noindex/draft/missing → [])
ℹ tests 11  suites 4  pass 11  fail 0

--- FULL GATE -----------------------------------------------------------------------------
$ npm run check     → Result (519 files): 0 errors, 0 warnings, 4 hints
                      (deprecation warnings shown are pre-existing docs/ brand-workbook JS)
$ npm run build     → [build] 5 page(s) built  (unwired components add no routes)
$ npm run verify    → verify: ok

--- npm test (whole suite) ----------------------------------------------------------------
2 failures, BOTH owned by the parallel T11 task (its untracked src/layouts/{Commercial,
CaseStudy}Layout.astro + its edits to tests/shell.test.mjs):
  ✖ CommercialLayout preserves the shell a11y contract and emits a Service graph
  ✖ CaseStudyLayout preserves the shell a11y contract and emits an Article graph
Confirmed in ISOLATION (`node --test tests/shell.test.mjs` → 24 pass / 2 fail), so this is
NOT the known parallel-build race — it is T11's in-progress state in the shared tree.
Grep of failing names: none outside layout/casestudy/commercial. My components are unwired,
so they cannot affect these probes. My task's files are all green; I commit only mine.
