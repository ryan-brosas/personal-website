Task 8 — Evidence, freshness, and relationship policy
Plan: seo-geo-authority-refactor
Date: 2026-07-25

SUMMARY
Added three pure, Node-importable policy-kernel modules (TDD, test-first in
tests/policy.test.mjs):
- src/lib/relationships.ts — typed {collection,id} ref + getRelatedList() UI-safety
  helper that DROPS non-public targets (returns [], no throw) by reusing the
  UNCHANGED public->public-only resolveRelationship. Never weakens the kernel.
- src/lib/evidence.ts — ClaimRecord -> SourceRecord resolution against the T7
  public-safe source registry (src/data/sources.json). Fail-closed: non-approved
  claim, no sources, or a missing source id => unresolvable. assertClaimResolvable
  is the throwing BUILD GATE. resolvePublicClaim additionally rejects internal-only
  sources backing a public claim (design §12.5).
- src/lib/freshness.ts — INV-13: nextModifiedAt bumps modifiedAt=now ONLY for a
  substantive change (body-text/heading/data-value/new-section); formatting/
  frontmatter-meta/typo-fix never manufacture freshness. Plus isExpired (vs
  expiresAt), isReviewStale (vs reviewedAt + interval), reviewStateFor
  (unreviewed|reviewed|stale). Pure: `now` injected, no Date.now() in logic.

CONSTRAINTS HONORED
- src/lib/publishing.ts NOT modified. resolveRelationship's public->public-only
  rejection semantics preserved EXACTLY — a T8 test re-asserts it still returns
  {ok:false} for a noindex target and {ok:true} for public.
- The empty-list behavior is a UI-safety fallback (sequencing/empty), NOT a policy
  change: a public page still never renders a link to a non-public target.
- No enums/namespaces (erasableSyntaxOnly): all closed sets are string-literal
  unions + module-level readonly arrays.
- Type-only imports use `import type` (verbatimModuleSyntax): RelationshipTarget/
  CollectionsMap from publishing.ts, PublicationDates from content-schemas.ts.
- Did NOT touch route-registry.ts, routes.ts, content.config.ts.

RECONCILIATION (T7 overlap)
- evidence.ts defines minimal local ClaimRecord/SourceRecord mirroring design §12.5
  (source permission public|redacted|internal-only; claim kind fact|metric|
  testimonial|interpretation|proposal; claim status approved|blocked|retired).
  T7 owns canonical entity/source/claim types in src/config/entities.ts. Documented
  in-file: if T7 lands a shared type, import it and delete the local shapes.
- Registry keyed by source id => matches src/data/sources.json object shape ({} today).

TDD (RED then GREEN)
RED (before implementing the modules):
  $ node --test tests/policy.test.mjs
  Error [ERR_MODULE_NOT_FOUND]: Cannot find module
  '/home/ryan/repo/personal-website/src/lib/relationships.ts'
  imported from tests/policy.test.mjs
  (correct reason: behavior missing, not a broken test)

GREEN — full composite gate:
  $ npm run check   -> Result (508 files): 0 errors, 0 warnings, 4 hints (pre-existing docs/ hints)
  $ npm test        -> tests 182, pass 182, fail 0
  $ npm run build   -> 5 page(s) built; Complete!
  $ npm run verify  -> verify: ok

T8 suites (all pass):
  ▶ T8 relationship helper (getRelatedList) — 4 tests
  ▶ T8 evidence policy (claim -> source resolution) — 8 tests
  ▶ T8 freshness policy (INV-13 + stale detection) — 7 tests

ACCEPTANCE (plan QA scenarios)
- happy: public->public relationship resolves ok (getRelatedList keeps the public
  ref); an approved claim resolves to its source.
- failure: public->noindex yields an EMPTY related list, build stays green
  (5 pages, verify ok); a claim referencing a missing source id fails the resolver
  and assertClaimResolvable throws.

FILES
  A src/lib/relationships.ts
  A src/lib/evidence.ts
  A src/lib/freshness.ts
  M tests/policy.test.mjs   — imports + 3 T8 describe blocks (19 tests)
