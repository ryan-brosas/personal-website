{
  "slug": "m2-accessibility-acceptance",
  "title": "M2 Accessibility Acceptance (Child 6 of Plan 03)",
  "created": "2026-07-22",
  "status": "in-progress",
  "parent": "m2-accessible-core-shell",
  "depends_on": ["m2-contact-page"],
  "blocks": [],
  "success_criteria": [
    {
      "id": "sc-1",
      "description": "Browser evidence is real, not inferred: capture script runs and produces browser evidence (screenshots/keyboard traces/console+network logs) for all 5 routes across keyboard, reflow 360/768/desktop, 200% zoom, reduced-motion, no-JS, contrast",
      "verify": "node scripts/a11y-capture.mjs runs; evidence artifacts exist under .playwright-mcp/m2-accessibility-acceptance/"
    },
    {
      "id": "sc-2",
      "description": "acceptance.md satisfies parent close-2: exists in P02A format with environment, route/viewport matrix, per-check results, BLOCKED screen-reader section, evidence paths, resume guard, decision, exact tested commit hash matching the audited code commit",
      "verify": "parent close-2 (m2-accessible-core-shell/plan.md:60-68) — acceptance.md recorded commit hash matches audited HEAD"
    },
    {
      "id": "sc-3",
      "description": "Static gates stay green (no code changed by the acceptance)",
      "verify": "npm run check && npm test && npm run build && npm run verify all exit 0"
    }
  ],
  "tasks": [
    {
      "id": "a1",
      "title": "Accessibility evidence capture",
      "category": "audit",
      "end_state": "A reproducible capture script (Playwright via npx, no new deps) builds the site (isolated pattern) and captures real browser evidence across /, /about/, /services/, /contact/, /404.html for: keyboard Tab/Shift+Tab order + visible focus + skip-link target + Escape/focus-return on the nav toggle; reflow at 360/768/desktop (no horizontal scroll, no clipped content); 200% zoom (real browser zoom); reduced-motion emulation; no-JS (content + nav usable with script disabled); console errors + network 4xx harvest; color-contrast spot-check of token pairings. Evidence binaries written under .playwright-mcp/m2-accessibility-acceptance/ (gitignored). Screen-reader smoke attempted and recorded as BLOCKED (no reader). No production code changed; if a defect is found, STOP and escalate (do not silently fix).",
      "depends_on": [],
      "parallel": false,
      "files": ["scripts/a11y-capture.mjs"],
      "verification": [
        "Evidence capture (not TDD): node scripts/a11y-capture.mjs runs and produces evidence artifacts under .playwright-mcp/m2-accessibility-acceptance/ (keyboard traces, per-viewport screenshots, zoom + reduced-motion + no-JS captures, console/network log)",
        "No code changed: npm run check && npm test && npm run build && npm run verify exit 0. Commit: chore(m2): add accessibility evidence capture script"
      ]
    },
    {
      "id": "a2",
      "title": "Acceptance evidence record",
      "category": "evidence",
      "end_state": ".opencode/artifacts/m2-accessibility-acceptance/acceptance.md exists in P02A format: header metadata (Created/Status/Accepted at/Decided by + audited commit hash), Environment, route/viewport matrix, per-check PASS results, a dedicated BLOCKED screen-reader section with re-open trigger, Evidence paths, Resume guard, Decision. The recorded commit hash equals the audited code HEAD (the commit immediately before the acceptance.md commit). No production code changed.",
      "depends_on": ["a1"],
      "parallel": false,
      "files": [".opencode/artifacts/m2-accessibility-acceptance/acceptance.md"],
      "verification": [
        "Evidence record: acceptance.md has all P02A sections; git rev-parse HEAD (audited code commit, i.e. the commit captured in A1, immediately before the acceptance.md commit) matches the hash recorded in acceptance.md",
        "No code changed: npm run check && npm test && npm run build && npm run verify exit 0. Commit: docs(m2): record accessibility acceptance (screen-reader BLOCKED)"
      ]
    }
  ]
}
