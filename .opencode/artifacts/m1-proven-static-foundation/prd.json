{
  "bead": "m1-proven-static-foundation",
  "title": "M1 Proven Static Foundation (Plan 01 — Policy-Kernel Tracer)",
  "created": "2026-07-22",
  "status": "complete",
  "completed_at": "2026-07-22",
  "depends_on": ["m0-execution-ready-baseline"],
  "blocks": ["m2-accessible-core-shell"],
  "related_tracks": [
    {
      "id": "p02a-homepage-prototype",
      "spec": ".opencode/artifacts/homepage-art-direction/",
      "parallel_with": ["m1"],
      "note": "Signal Path prototype + no-JS mobile-nav repair + visual acceptance; disjoint files, runs in parallel"
    },
    {
      "id": "p02b-homepage-canonicalization",
      "spec": ".opencode/artifacts/homepage-art-direction-canonicalization/",
      "gates": ["plan-03-task-1-visual-integration", "plan-04-choreography"],
      "note": "Does not gate M1"
    }
  ],
  "success_criteria": [
    {
      "id": "sc-1",
      "description": "Pinned Astro 5.18.2 / TS 6.0.3 / @astrojs/check 0.9.9 baseline installs and builds an empty static site with strict TypeScript and trailing-slash always",
      "verify": "npm ci && npm run check && npm run build; dist/ exists; astro.config.mjs has output 'static', trailingSlash 'always', placeholder site"
    },
    {
      "id": "sc-2",
      "description": "Publishing contracts compile and pass policy tests: shared Zod schemas (visibility draft|public|noindex defaulting to draft, evidence variants Verified requires sourceId, Proposed requires a trade-off, Open requires missing proof + blocked decision), typed relationships, and the content adapter importing those schemas",
      "verify": "npm run check exits 0 on src/lib/publishing.ts, src/lib/routes.ts, src/lib/discovery.ts, src/content.config.ts; npm test asserts the invariants against injected synthetic state"
    },
    {
      "id": "sc-3",
      "description": "Policy tests pass against public/draft/noindex fixtures: no draft route, correct visibility enum, evidence-variant invariants, relationship target validation",
      "verify": "npm test exits 0; tests/policy.test.mjs asserts the invariants"
    },
    {
      "id": "sc-4",
      "description": "Discovery output excludes draft and noindex; one self-canonical per indexable page (proved by the test-only fixture); correct trailing slashes; robots is visibility-independent and references the configured origin; verifier is read-only, parameterized for root vs fixture build, and passes against the placeholder site",
      "verify": "npm run build && npm run verify exit 0; dist/sitemap.xml + dist/robots.txt only (no index); sitemap has no draft/noindex; fixture build emits one self-canonical https://example.com/probe/; robots has no Disallow; verify-build.mjs read-only (no mutation)"
    }
  ],
  "tasks": [
    {
      "id": "t1",
      "title": "Pinned Astro 5 baseline skeleton",
      "category": "scaffold",
      "end_state": "A pinned, strict-TS Astro static project installs and builds an empty site with placeholder site and trailing-slash always.",
      "depends_on": [],
      "parallel": false,
      "conflicts_with": [],
      "files": [
        "package.json",
        "astro.config.mjs",
        "tsconfig.json",
        "src/env.d.ts",
        "package-lock.json"
      ],
      "files_authored": ["package.json", "astro.config.mjs", "tsconfig.json", "src/env.d.ts"],
      "files_generated": ["package-lock.json"],
      "verification": [
        "npm ci exits 0",
        "npm run check exits 0 (strict TS, no errors)",
        "npm run build exits 0 and dist/ exists",
        "rg -n '\"static\"|trailingSlash|example.com' astro.config.mjs confirms static output, trailing-slash always, placeholder site"
      ]
    },
    {
      "id": "t2",
      "title": "Publishing policy (TDD: tests first)",
      "category": "contracts",
      "end_state": "Shared Zod schemas and pure functions for visibility (default draft), evidence variants, date semantics, and relationship-target resolution are encoded and pass policy tests; the empty evidence registry is committed.",
      "depends_on": ["t1"],
      "parallel": false,
      "conflicts_with": [],
      "files": ["tests/policy.test.mjs", "src/lib/publishing.ts", "src/data/sources.json"],
      "verification": [
        "npm test exits 0 (policy tests written RED first, then GREEN)",
        "tests assert: default visibility draft; routable public/noindex vs discoverable public-only; Verified requires sourceId, Proposed requires a trade-off, Open requires missing proof + blocked decision; unknown sourceId rejected against an injected synthetic registry; distinct publish/updated/reviewed date fields; relationship-target resolver rejects hidden/missing/draft/noindex targets against an injected synthetic collection map",
        "npm run check exits 0 (publishing.ts has no astro:* import, no any, no throw for domain)",
        "src/data/sources.json is exactly {} (empty public-safe evidence registry, NOT content fixtures)"
      ]
    },
    {
      "id": "t3",
      "title": "Routes and content adapter (TDD: tests first)",
      "category": "contracts",
      "end_state": "The public-route inventory and canonical helpers are encoded, and the content adapter registers launch collections (empty) plus the settings singleton against the shared schemas; the adapter loads and type-checks clean.",
      "depends_on": ["t2"],
      "parallel": false,
      "conflicts_with": [],
      "files": [
        "tests/policy.test.mjs",
        "src/lib/routes.ts",
        "src/content.config.ts",
        "src/content/settings/site.json"
      ],
      "verification": [
        "npm test exits 0 (route tests written RED first, then GREEN)",
        "tests assert: route-contract invariants and canonicalHref(path, site) produces absolute trailing-slash URLs from the configured site",
        "npm run check exits 0 on src/content.config.ts (imports shared schemas from publishing.ts; does not restate them)",
        "npm run build exits 0 (adapter module load; empty-glob warnings are expected and documented)",
        "src/content/settings/site.json is exactly {} (empty container; Plan 03 owns the first substantive record)"
      ]
    },
    {
      "id": "t4",
      "title": "Discovery, canonical proof, and read-only verifier (TDD: tests first)",
      "category": "discovery",
      "end_state": "Sitemap and robots render from the public-route inventory; a test-only Astro fixture proves one self-canonical per indexable page with trailing slash; the verifier is read-only, parameterized for root vs fixture build, and passes against the placeholder site.",
      "depends_on": ["t3"],
      "parallel": false,
      "conflicts_with": [],
      "files": [
        "tests/policy.test.mjs",
        "src/lib/discovery.ts",
        "src/pages/sitemap.xml.ts",
        "src/pages/robots.txt.ts",
        "tests/fixtures/policy-site/astro.config.mjs",
        "tests/fixtures/policy-site/src/pages/probe.astro",
        "scripts/verify-build.mjs"
      ],
      "verification": [
        "npm test exits 0 (renderSitemap/renderRobots + fixture canonical + verifier cases written RED first, then GREEN)",
        "npm run build exits 0 and generates exactly dist/sitemap.xml and dist/robots.txt (NOT a sitemap index)",
        "renderSitemap: deterministic XML, public-only, draft/noindex excluded, empty input emits a well-formed <urlset> (local placeholder; NOT release-valid)",
        "renderRobots: visibility-independent, no Disallow for any route, emits an absolute slashless Sitemap: <site>/sitemap.xml line",
        "fixture build (tests/fixtures/policy-site copied to temp) emits dist/probe/index.html with exactly one <link rel=\"canonical\" href=\"https://example.com/probe/\">",
        "npm run verify exits 0 read-only (snapshot output tree before/after to prove no mutation); fails on missing/duplicate/wrong-origin canonical, HTML slash mismatch, draft/noindex sitemap leak, unexpected/missing route, endpoint mismatch",
        "@astrojs/sitemap is NOT in package.json dependencies"
      ]
    }
  ]
}
